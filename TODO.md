# TODO

Dev order per `SPEC.md` §16. Don't reorder without a reason — step 7 (Reveal) is deliberately where the security-critical foundation gets proven before anything else gets built on top of it.

- [x] **1. SwiftUI shell + navigation skeleton** (§0) — `App/LifeVarsApp.swift`, `App/RootView.swift`
- [x] **2. `LifeVar` model** (§14) — `Models/LifeVar.swift`. Local encrypted *store* (SwiftData) is not built yet — deferred to step 5, where it's first actually needed.
- [x] **3. Keychain key management** (§15) — `Security/KeychainDEKStore.swift`, `Security/CryptoBox.swift`
- [x] **4. Face ID/passcode session gate** (§2) — `Security/SessionManager.swift`, `Lock/LockScreenView.swift`. Verified live in simulator: enable → unlock → manual lock → background/foreground relock all round-trip correctly.
- [x] **5. Add/Edit/Delete** (§6, §7) — `Data/Persistence.swift` (SwiftData store, excluded from backup per §15.2), `Data/LifeVarStore.swift` (encrypt/decrypt CRUD against the DEK), `Variables/AddVariableView.swift` (two-step wizard), `Variables/EditVariableView.swift` (swipe-to-edit, reveal-to-edit gated by `requireUnlockedSession()`). §1.4 first-LifeVar prompt wired into `HomeView`. Verified live: add → encrypt → persist → relaunch → decrypt returns the exact original value; edit and delete both round-trip correctly.
  - Free-tier 5-item cap intentionally **not** implemented yet — that's step 12 (Paywall); Add currently allows unlimited items.
  - Debugging note for future device-automation work: a SwiftUI `List` row `Button` only got taps delivered by `idb ui tap` after adding `.contentShape(Rectangle())` — without it, the row's accessibility frame reported the full row width but the actual hit-tested area was only the intrinsic content (icon+text), silently swallowing taps outside that. Not a `swipeActions` issue as first suspected.
- [x] **6. Home + search** (§3) — `Models/Matcher.swift` (§9.2: normalize, strip stop-phrases, exact/substring/token confidence tiers — built now rather than as a throwaway naive filter, since voice in step 9 reuses the same function). Live search bar, no-match "Save as new" card, Return-key-on-single-match reveal, all in `HomeView`. Verified live: "vin" narrows to exactly "Audi VIN" excluding "EIN"; empty search offers "Save as new" and Return reveals the sole match.
- [x] **7. Reveal screen** (§5) — `Reveal/RevealView.swift`, replacing the step-5 stopgap sheet with a `fullScreenCover`. Countdown (default 30s), re-mask-in-place + "Reveal Again" on timeout (not dismiss — backgrounding is the real boundary, §2.3), Copy via `UIPasteboard` with 60s `.expirationDate`, `UIScreen.capturedDidChangeNotification` screen-recording guard, `onChange(of: session.isUnlocked)` safety-net dismiss. Verified live end to end including the full 30s timeout → re-mask → Reveal Again → fresh decrypt with no second Face ID prompt.
- [x] **8. Disambiguation** (§10) — `Voice/DisambiguationView.swift`, "Which \<X\>?" sheet shown when a voice transcript resolves to more than one top-confidence candidate; heading derived from `Matcher.normalize(transcript).capitalized`. Auth happens after selection (picking a row presents `RevealView`, which gates itself), not before the list shows.
- [x] **9. Local voice transcription** (§4) — `Voice/VoiceRecognizer.swift`: `SFSpeechRecognizer` + `AVAudioEngine`, `requiresOnDeviceRecognition = true` with no server fallback path at all (not even as an error case), ~1.2s silence-based end-of-speech via a resettable timer, mic button in `HomeView` swaps the search field for a "● Listening..." indicator per §4. Result routes through the same `Matcher` as typed search: single top match → reveal immediately, multiple → disambiguation (step 8), none → reuses the typed-search no-match card with the transcript pre-filled.
  - Verified live: mic tap correctly triggers the system Speech Recognition permission dialog with our exact usage-description copy; on grant, `AVAudioEngine` starts recording (confirmed via device logs — `AudioDeviceStart (err 0)`); the 1.2s silence timer correctly tears down and returns to idle with no crash when no speech arrives, and the mic is cleanly reusable afterward.
  - **Not verified**: actual speech→text→match→reveal, since the iOS Simulator has no real microphone input available in this environment (no host-audio passthrough) and `idb`/`simctl` have no audio-injection capability. The transcript-handling logic (`handleVoiceResult`) reuses the same `Matcher` already verified via typed search in step 6, so confidence is high, but this specific path needs a real device (or a Simulator with host mic passthrough) to fully confirm.
- [x] **10. Auto-hide + background locking polish** (§2.3, §5) — `App/AppSwitcherOverlay.swift`, shown via a `ZStack` in `LifeVarsApp` keyed to `scenePhase != .active` (covers Home, Reveal, Add/Edit, even the lock screen itself — not tied to auth state, since a snapshot can leak in-progress form text just as easily as a revealed value). Also closed a gap: `EditVariableView` didn't dismiss on background-lock the way `RevealView` already did — added the same `onChange(of: session.isUnlocked)` safety net. Auto-hide duration was already Settings-driven (`UserSettings.autoHideSeconds`) since step 7, just no Settings UI yet to change it (that's step 12).
  - Verified live: full background → relock → foreground → auto-unlock → Home cycle still works correctly with the overlay in place, no regression.
  - **Not verified**: the overlay's actual visual appearance in the app-switcher snapshot — `idb`/`simctl` have no way to invoke or screenshot the multitasking UI itself, only the foreground app. The mechanism (`scenePhase`-driven `ZStack` overlay) is standard and was exercised indirectly via the background/foreground cycle above, but the snapshot moment itself is unverified.
- [x] **11. Clipboard handling** (§5) — already done as part of step 7 (`RevealView.copy()`): `UIPasteboard.setItems` with a 60s `.expirationDate`, native API rather than a custom timer.
- [x] **12. Settings + Paywall** (§11) — `Settings/SettingsView.swift` (Unlock Method picker with the §2.4 trade-off caption, Auto-hide picker, Lock-on-background toggle, Appearance segmented control, About/Privacy/Security), `Settings/PaywallView.swift`, `Store/StoreManager.swift` (StoreKit 2, non-consumable), `LifeVars.storekit` (local test config — XcodeGen wired it straight into the scheme's `StoreKitConfigurationFileReference`, confirmed in the generated `.xcscheme`). Free-tier 5-item gate wired into `AddVariableView.save()`.
  - Verified live: every Settings row renders and matches spec; gear icon opens it from Home; added items up to 5, a 6th correctly opened the paywall instead of saving instead of silently failing.
  - Found and fixed a real bug while testing: `Product.products(for:)` returning an empty array (not a thrown error) left the paywall spinning forever with no explanation — now surfaces "Pro isn't available right now."
  - **Not verified**: an actual StoreKit purchase completing. Launching via `simctl launch` (this environment's only install path) bypasses Xcode's scheme-driven launch, so the local `.storekit` config never attaches — `Product.products(for:)` legitimately finds nothing. This needs an Xcode Run-button or `xcodebuild test` launch to exercise for real.
- [x] **13. Siri/App Intents** (§13, stretch) — `Intents/FindLifeVarIntent.swift`, `Voice/PendingSiriQuery.swift`. Hit a real platform constraint: `AppShortcut` phrases can only embed `AppEntity`/`AppEnum` parameters, not free-text `String` — and giving Siri an enumerable entity of LifeVar names would mean exposing them outside a session, which the security model doesn't allow. Resolved with the two-step voice-prompt pattern (`requestValueDialog`) instead of embedding the query in the phrase. The intent hands the app nothing but a raw query string via `PendingSiriQuery`, checked in `HomeView` on `.onAppear` and `UIApplication.didBecomeActiveNotification`, then run through the exact same `handleVoiceResult` path as in-app voice — no shortcut, no bypass, and structurally no way for the intent process to touch a decrypted value since it never holds the DEK.
  - Verified: builds clean, `Metadata.appintents` actually generated in the build product (confirming real registration, not just no compile errors), app launches fine with the intent code present.
  - **Not verified**: actual Siri invocation — Siri does not function in the iOS Simulator at all, on any app, regardless of tooling. This needs a real device.
- [x] **14. Accessibility pass** — Home row and `RevealView`'s masked state both had VoiceOver reading masked dots literally; fixed with `.accessibilityHidden(true)` on the dot text plus a combined `.accessibilityLabel` ("Audi VIN, hidden value" / "Value hidden"), confirmed via the accessibility tree, not just visually. Dynamic Type on the Reveal value was already correct by construction — it was never a fixed pixel size, just a scalable `.title3` style, so nothing needed to change there.
- [x] **15. Security review** — re-checked §15.2's edge cases against actual behavior rather than just the spec's prediction of it:
  - Added the "no passcode set" proactive check §15.2 asked for (`SessionManager.unlock()` now probes `canEvaluatePolicy` before attempting the real fetch, so that failure mode gets an explanation instead of an endless retry loop) — verified it doesn't regress normal unlock.
  - Verified live: full uninstall + reinstall wipes everything back to fresh onboarding, matching the documented "reinstall = data loss" behavior.
  - **Not verified** (need a real device): Face ID re-enrollment invalidating `.biometryCurrentSet` access, and an actual device-restore leaving the store excluded from backup as intended. Both are single, well-isolated native-API calls (`SecAccessControlCreateWithFlags` flags, `isExcludedFromBackup`) that were code-reviewed against Apple's documented behavior rather than exercised, since Simulator doesn't meaningfully model either scenario.
- [ ] **16. TestFlight** — **not attempted.** This needs your actual Apple Developer Program membership, an App Store Connect app record, a real signing team/certificate, and an upload — all real-world actions tied to your account that I can't responsibly do without you present.

## Acceptance test (SPEC.md §17)

All the pieces it depends on are now built (steps 1–15), but the test itself needs a real device — see step 9's and step 13's verification notes. This is the actual definition of done, not step 16:

1. Install. Add `Audi VIN = WAU123456789`.
2. Close LifeVars. Reopen → locked, not Home.
3. Face ID → Home.
4. Tap mic, say "What's my Audi VIN?"
5. Within ~1 second: `Audi VIN / WAU123456789 / Copy` — no second Face ID prompt.
6. Close the app. Reopen → locked again.

## Post-plan additions

Features added after the original 16-step plan closed out, in response to direct product asks.

- [x] **Manual category picker + optional grouped Home list** (SPEC.md §3.0.1, §6.2, §7) — `Category.custom` renamed to `.other` throughout (code and UI; "custom" read oddly once it became something a user picks from a menu). Add and Edit both got a `Picker` (`.menu` style) that defaults to the auto-classified category but can be overridden directly; once manually set, further name edits on the same screen no longer silently override the choice (`isCategoryManuallySet` in `AddVariableView`). Home got a persisted ("Category") toggle above the list that switches between the flat recency-sorted list (default) and sections grouped by `Category.allCases` order, skipping empty categories.
  - Verified live: "Netflix Password" (unclassifiable) correctly defaulted to "Other," manually overridden to "Identity" via the dropdown, and stuck. Added Tesla VIN / Audi VIN / EIN alongside it, toggled grouping on — got IDENTITY / VEHICLE (Tesla VIN + Audi VIN together) / BUSINESS sections in that order — toggled off, back to the flat list unchanged.
- [x] **Category-driven value formatting** (SPEC.md §9.1 — SSN → `123-45-6789`, EIN → `12-3456789` on the Reveal screen) — `Models/LifeVar.swift`'s `ValueFormat` enum (`.ssn`, `.ein`) with `apply(to:)`, a pure cosmetic transform (digit-count gated, stored value never mutated). `Classification.swift`'s rule table now carries a `format` alongside category/aliases; `RevealView` displays `item.format?.apply(to: value) ?? value` while `copy()` still copies the raw unformatted value.
  - **Not verified live**: builds clean but wasn't exercised end-to-end (add an SSN-classified item → Reveal → check the dashes render) in this stretch — worth a quick pass before considering it done-done.
- [x] **Expiration metadata + temporary (delete-on-expiry) items** — `LifeVarMetadata` gained `expiresAt`/`deleteOnExpiration`; `Notifications/ExpirationNotifier.swift` schedules a generic, non-identifying local reminder (`reminderDate = max(now+60s, expiresAt - 90 days)` — never names the item, matching the never-leak-plaintext invariant) for items that just expire, and cancels it on delete/edit. Items marked delete-on-expiration are swept opportunistically on every unlock (`LifeVarStore.sweepExpiredTemporaryItems`, called from `reload()`) rather than via a background task or server — matches the "hotel key code" use case (a temporary value that should just vanish, not merely warn). Add/Edit both got an Expiration section (Never / 1 Day / 1 Week / Custom Date, plus a "delete automatically when expired" toggle); `RevealView` shows an "Expires …" or "Deletes …" caption when set.
  - **Not verified live**: build-verified only. The sweep logic and notification scheduling weren't exercised against a real expiring item in the simulator this session.
- [x] **Emergency Info** — a deliberately narrow, disclosed exception to the Face-ID-everywhere rule, modeled on Apple's own Medical ID trade-off. `Security/EmergencyKeyStore.swift` mirrors `KeychainDEKStore` but with `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` and **no** biometric/passcode access control at all — isolated to only the items a user explicitly opts in via the new "Emergency Access" toggle in Add/Edit. Opted-in items get a second, minimal encrypted shadow copy (`LifeVar.emergencyPayload`, `Emergency/EmergencyPayload.swift` — just `{name, value}`, no aliases/format/category) sealed under this weaker key. `Emergency/EmergencyAccessView.swift` is reachable from `LockScreenView`'s "Emergency Info" button with **no session gate at all**, showing an orange "Visible without Face ID" banner. Deliberately no screen-recording guard on this one screen — an emergency responder may need to show it to someone.
  - **Not verified live**: build-verified only.
- [x] **Apple Watch companion** — thin-client relay architecture, chosen deliberately over giving the Watch its own DEK copy: the Watch never holds a key or decrypted data, every ask is a live round-trip to the already-unlocked phone. `Connectivity/PhoneConnectivitySession.swift` (phone side, `WCSessionDelegate`) receives `["query": String]`, returns `locked` / `notFound` / `ambiguous(candidates:)` / `ok(name, value:)` via the same `Matcher`/`LifeVarStore.revealValue` path as everything else — no separate matching logic. New `LifeVarsWatch` watchOS target (`WatchConnectivityManager.swift`, `ContentView.swift`) — ask a question, get a name + value + Done, no scrolling, no folders, matching the product's "one tiny fact, fast" pitch for the Watch. `project.yml` gained a `watchOS: "10.0"` deployment target and the new target embedded into `LifeVars` via `dependencies: [{target: LifeVarsWatch, embed: true}]`.
  - Verified: both the `LifeVarsWatch` and `LifeVars` schemes build clean with the watch app embedded (`Debug-watchsimulator/LifeVars.app` and `Debug-iphonesimulator/LifeVars.app/Watch/LifeVars.app` both present in the build products).
  - **Not verified live**: actual Watch↔Phone message exchange. WatchConnectivity between two Simulator instances isn't something this environment could drive end-to-end — needs a real paired Watch + iPhone.
- [x] **App icon + launch screen** — the project had no `Assets.xcassets` at all going into this (default gray icon, blank `UILaunchScreen: {}`). First pass: a hand-rendered `{ = }` glyph (via a small AppKit/CoreGraphics script using `NSFont.monospacedSystemFont`, not hand-authored SVG text, so it rasterizes identically to how it'll actually render) on a solid indigo square, wired as a single-size (1024×1024) universal App Icon for both the iOS and watchOS targets plus a small `LaunchIcon` imageset for the launch screen. Superseded at the user's direction by a logo they generated separately (fingerprint/shield "V" monogram) — same asset-catalog wiring, just a different source image (`lifevars_icon_perfect_square.png`).
  - Verified: `Assets.car` compiles into both the iOS and watchOS app bundles; both schemes build clean with `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` set.
  - **Open concern, not yet resolved**: SPEC.md §12 originally called for avoiding padlock/shield/fingerprint imagery specifically *because* every security app uses it; the chosen logo is exactly that. Also flagged and still unconfirmed — the fine fingerprint ridge linework and glow effect are the kind of detail that typically blurs into mush at real Home Screen (60pt) and Settings-row (29pt) icon sizes. Worth an actual look at real scale (device or Simulator Home Screen, not just the Xcode preview) before calling this final.
- [x] **Free tier limit lowered from 5 to 3** — `Store/StoreManager.freeItemLimit` is now the single source of truth (was two independent hardcoded `5`s in `AddVariableView.save()`'s paywall gate and `SettingsView`'s "X of 5 used" label — now both read the constant, so they can't drift again). SPEC.md §6.2/§11/§11.1 updated to match.
- [x] **Lock Screen & Control Center widgets (iOS 18)** — new `LifeVarsWidgets` WidgetKit extension target, embedded into `LifeVars` the same way the watch app is.
  - **Pinned Variable widget** (`Widgets/PinnedVariableWidget.swift`) — Lock Screen accessory widget (`.accessoryCircular`/`.accessoryRectangular`) for one LifeVar the user pins in Add/Edit (`isPinned` on `LifeVarMetadata`, single-pin enforced by `LifeVarStore.unpinOtherRecords` so pinning one silently unpins any other). The widget is deliberately a shortcut, not a display — it shows a generic lock glyph and "Pinned"/"Not Set", never the item's name, category, or value. It only ever learns the pinned item's *id* via `Shared/AppGroupBridge.swift` (App Group `group.com.serhansari.LifeVars`, written by `LifeVarStore.reload()`), consistent with `LifeVar.id` already being the one field considered safe to store in the clear (§14). Tapping it opens the app via a `lifevars://reveal?id=` deep link (`onOpenURL` in `LifeVarsApp`, drained by `HomeView.checkPendingReveal()`) and still goes through the full Face ID session gate before RevealView shows anything.
  - **"Ask LifeVars" Control Center control** (`Widgets/AskLifeVarsControl.swift`, iOS 18 `ControlWidget`, gated with `if #available` inside the `WidgetBundle` so the rest of the extension still supports iOS 17) — same `openAppWhenRun` pattern as `FindLifeVarIntent` (§13): the control's `AppIntent.perform()` runs in the extension process and can't touch a DEK even by mistake, it just flips a flag in the App Group (`WidgetBridge.setPendingActivateVoice`), which `HomeView.checkPendingVoiceActivation()` drains on next foreground and starts listening through the same `VoiceRecognizer`/`Matcher` path as tapping the mic by hand.
  - Verified: all three schemes (`LifeVars`, `LifeVarsWatch`, `LifeVarsWidgets`) build clean with the extension embedded (`LifeVarsWidgets.appex` present under `LifeVars.app/PlugIns`), including the mixed `Widget`/`ControlWidget` `WidgetBundle`.
  - **Not verified live**: neither a Lock Screen widget nor a Control Center control can be added to their respective galleries via `simctl`/`idb` — no CLI path exists for that interaction. Needs a real device (or at minimum an Xcode Run + manual Lock Screen/Control Center edit) to confirm the widget actually appears, the tap deep-link fires end to end, and the control starts listening. The App Group entitlement itself is also unverified beyond Simulator's more lenient enforcement — real device/TestFlight provisioning needs this App Group actually registered in your Apple Developer account, same caveat class as TestFlight itself (step 16).
- [x] **Headless Siri check** (SPEC.md §19.5) — a second, separate App Intent (`Intents/CheckLifeVarIntent.swift`, `openAppWhenRun = false`) alongside `FindLifeVarIntent`, built after explicitly discussing and rejecting a notification-based version (would've shown on the Lock Screen unauthenticated, persisted in Notification Center, and could mirror to other devices via Handoff — a real leak, not a style choice). This one authenticates with its own throwaway `LAContext`/`KeychainDEKStore` fetch (never touching `SessionManager`), decrypts via a new shared `Data/LifeVarLookup.swift` helper (factored out of the same decrypt-and-match shape `LifeVarStore.reload()` already used, via a new `DecryptedIndexEntry(id:metadata:)` convenience init both now call), and shows the result as an inline Siri snippet (`LifeVarSnippetView.swift`) — never a notification.
  - Real platform snag: the `.result(view:)` factory that makes an `AppIntent`'s result conform to `ShowsSnippetView` isn't in the base `AppIntents` module at all — it only appears via `AppIntents`' cross-import overlay with `SwiftUI`, meaning the file needs `import SwiftUI` even though nothing in it references a screen-visible view directly. Silently missing that import doesn't error on the import line — it errors much later, confusingly, as "argument passed to call that takes no arguments" on the `.result(view:)` call itself. Confirmed via direct `swiftc -typecheck` probing against the SDK's `.swiftinterface`, not guessed.
  - Registered as its own Siri phrase ("Ask LifeVars to check something" / "Check something in LifeVars") distinct from `FindLifeVarIntent`'s phrasing, so Siri can route to either.
  - Verified: builds clean (`LifeVars` scheme), Metadata.appintents generation succeeds for both intents.
  - **Not verified live**: Siri doesn't run in Simulator at all (same limitation noted for `FindLifeVarIntent` in step 13) — needs a real device. Also genuinely untested: whether `LifeVarLookup`'s independent `ModelContext`/`ModelContainer` instance behaves correctly if invoked while the main app's own container is simultaneously open in another process state — reads only, so worst case is a save conflict on the `lastAccessedAt` write, not data corruption, but not exercised.
  - **Named, not hidden, trade-off**: this intent authenticates *whoever's face is in front of the phone*, and the Siri snippet is system UI outside LifeVars' control (unlike RevealView's auto-hide + no-screen-recording guard). Both are called out directly in SPEC.md §19.5, not glossed over.
